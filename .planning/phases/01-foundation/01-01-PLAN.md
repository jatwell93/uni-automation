---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/config.py", "src/models.py", "README.md"]
autonomous: true
requirements: [CONFIG-01, CONFIG-02, CONFIG-03, CONFIG-04]
must_haves:
  truths:
    - "User can create YAML config file with required fields (lecture URL, slide path, cookie file, output dir)"
    - "System validates config at startup and fails fast with clear error message if required field missing or invalid"
    - "System provides progress output at each pipeline stage (config load, auth test, download, validation)"
    - "User receives clear error message on config validation failure listing which field is missing/wrong"
  artifacts:
    - path: "src/config.py"
      provides: "Pydantic model + YAML loader for config validation"
      min_lines: 50
      exports: ["ConfigModel", "load_config"]
    - path: "src/models.py"
      provides: "Data classes for config, download metadata, validation results"
      min_lines: 30
      exports: ["LectureConfig", "DownloadResult"]
    - path: "config/example_week_05.yaml"
      provides: "Example config file for user reference"
      contains: ["lecture:", "paths:", "metadata:"]
    - path: "README.md"
      provides: "Setup instructions, config format documentation, examples"
      contains: ["Configuration", "YAML", "required fields", "example"]
  key_links:
    - from: "run_week.py"
      to: "src/config.py"
      via: "load_config() at startup"
      pattern: "config.*=.*load_config\\(config_file\\)"
    - from: "Pydantic model"
      to: "error messages"
      via: "validation_error handler"
      pattern: "except.*ValidationError.*clear.*message"
---

<objective>
Build the configuration infrastructure (YAML + Pydantic) and CLI entry point that validates all required fields at startup before any downloads begin.

Purpose: Foundation for all downstream phases; users can't proceed to auth or download without valid config. Fail-fast approach catches user errors early.

Output: Working config module, example YAML file, and documentation so users can create valid config files.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Pydantic config model with YAML loader</name>
  <files>src/config.py, src/__init__.py</files>
  <action>
Create a Pydantic model `ConfigModel` that validates lecture configuration. 

Required fields:
- `lecture.url`: HTTP/HTTPS URL (validate with pydantic validator or AnyUrl)
- `lecture.slide_path`: Path that exists on filesystem (validate with Path.exists())
- `paths.cookie_file`: Path to browser cookie JSON export (can be non-existent at config validation time, but path must be writable directory)
- `paths.output_dir`: Path where downloads go (validate writable with test write)

Optional fields with defaults:
- `metadata.course_name`: str (default: "Unknown Course")
- `metadata.week_number`: int (default: 1)
- `metadata.lecturer_name`: str (default: "")
- `metadata.timestamp`: str (default: current ISO timestamp)

Validation rules:
- All required fields must be present; raise ValidationError listing what's missing
- URL must be valid HTTP/HTTPS (not just any string)
- slide_path and output_dir must be valid Path objects
- output_dir must be writable (test with Path.mkdir(exist_ok=True) before saving config)
- Error messages must be clear and actionable (e.g., "Invalid URL: {url}. Must be https://...")

Create `load_config(config_file: str) -> ConfigModel` function that:
1. Reads YAML file using PyYAML
2. Validates with Pydantic
3. Catches YAML parse errors and ValidationError; re-raises with clear messages including:
   - Line number if YAML syntax error
   - Field name and validation rule if schema error
4. Returns validated ConfigModel instance

Handle exceptions:
- FileNotFoundError: "Config file not found: {path}"
- yaml.YAMLError: "Config file syntax error at line {line}: {error}"
- ValidationError: "Config validation failed:\n- {field}: {reason}\n..." (list all errors)

Create `src/__init__.py` to export ConfigModel and load_config at package level.
  </action>
  <verify>
pytest src/test_config.py::test_pydantic_model -xvs (test all validation rules)
pytest src/test_config.py::test_load_config_valid -xvs (test happy path)
pytest src/test_config.py::test_load_config_missing_field -xvs (test error messages)
pytest src/test_config.py::test_output_dir_writable -xvs (test writable check)
  </verify>
  <done>
ConfigModel fully validates required/optional fields with clear error messages. load_config() successfully loads valid YAML config and rejects invalid configs with helpful error text. All validation rules (URL format, path exists, writable) enforced.
  </done>
</task>

<task type="auto">
  <name>task 2: Create CLI entry point with config validation at startup</name>
  <files>run_week.py, src/logger.py, .planning/logs/.gitkeep</files>
  <action>
Create `run_week.py` as the single CLI entry point for the entire pipeline.

CLI interface:
```
python run_week.py <config_file>
```

Behavior:
1. Accept config_file as positional argument; validate that file exists before proceeding
2. Call load_config(config_file) to validate config
3. If config validation fails: Print error message to console + log to file, exit(1)
4. If config valid: Log "✓ Config validated" and proceed to auth phase (stub for now)
5. Exit with status code 0 on success, non-zero on errors

Logging setup in `src/logger.py`:
- Create `.planning/logs/` directory if missing (mkdir parents=True, exist_ok=True)
- Configure logging to both file + console
- File: `.planning/logs/week_XX.log` (where XX is week_number from config)
- Format: `%(asctime)s [%(levelname)s] %(message)s` (ISO timestamp, level, message)
- Console: INFO level, simple format (no timestamp)
- File: DEBUG level (capture everything)

CLI progress output (to console):
- "🔧 Loading config from {config_file}..."
- On error: "✗ Config validation failed:\n{error_details}"
- On success: "✓ Config validated (course={course}, week={week})"
- Next step indicators: "→ Next: Validating Panopto authentication..."

Error handling:
- Missing config file: "Config file not found: {path}"
- Config validation error: Pass through from load_config() with full error context
- Exit code: 0 on success, 1 on error

Structure:
```python
# run_week.py
def main():
    parser = argparse.ArgumentParser(description="Process lecture and generate study notes")
    parser.add_argument("config_file", help="Path to YAML config file")
    args = parser.parse_args()
    
    # Setup logging
    logger = setup_logging(config_file_path)  # from src/logger.py
    
    # Load + validate config
    try:
        config = load_config(args.config_file)
        logger.info(f"✓ Config validated: {config.metadata.course_name} week {config.metadata.week_number}")
    except Exception as e:
        logger.error(f"Config validation failed: {e}")
        print(f"✗ {e}")
        return 1
    
    # Placeholder: next steps (auth, download, etc.)
    logger.info("→ Next: Validating Panopto authentication...")
    
    return 0

if __name__ == "__main__":
    sys.exit(main())
```

Do NOT implement auth or download logic yet (those are in later plans). This task is scaffolding + config validation only.
  </action>
  <verify>
python run_week.py config/example_week_05.yaml (should succeed with ✓ Config validated message)
python run_week.py nonexistent.yaml (should fail with "Config file not found" message)
cat .planning/logs/week_05.log (should contain "[INFO] ✓ Config validated...")
  </verify>
  <done>
CLI entry point successfully loads and validates config. Clear error messages on missing/invalid files. Logging to file + console working. Next phase can call run_week.py and assume config is validated.
  </done>
</task>

<task type="auto">
  <name>task 3: Create example config file and documentation</name>
  <files>config/example_week_05.yaml, README.md</files>
  <action>
Create `config/example_week_05.yaml` as a complete, valid example that users can copy and customize:

```yaml
# config/example_week_05.yaml
# Example configuration for processing a lecture

lecture:
  # Full URL to Panopto video viewer page
  # Find this by navigating to the lecture in Panopto and copying the URL from your browser
  url: "https://panopto.university.edu/Panopto/Pages/Viewer.aspx?id=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
  
  # Path to PDF slides file (must exist)
  # Use forward slashes (/) even on Windows; pathlib handles conversion automatically
  slide_path: "slides/week_05.pdf"

paths:
  # Path to cookies JSON file (browser export)
  # Extract from browser: Chrome DevTools (F12) → Storage → Cookies → Select all → Copy → Save as JSON
  # See README.md for detailed instructions
  cookie_file: "cookies/panopto.json"
  
  # Directory where downloaded files will be saved
  # Must exist and be writable; created automatically if missing
  output_dir: "downloads/week_05"

metadata:
  # Optional fields (can be omitted; sensible defaults used)
  course_name: "Business Analytics"
  week_number: 5
  lecturer_name: "Prof. Smith"
  # timestamp: "2026-03-05 09:00"  # Optional; generated from current time if omitted
```

Add to `README.md`:

## Configuration

Phase 1 requires a YAML configuration file specifying the lecture URL, slides path, and authentication.

### Required Fields

- `lecture.url` — Full Panopto URL (copy from browser address bar)
- `lecture.slide_path` — Path to PDF slides file (must exist)
- `paths.cookie_file` — Path to browser cookie JSON export
- `paths.output_dir` — Where to save downloaded files

### Optional Fields

- `metadata.course_name` — Course name (default: "Unknown Course")
- `metadata.week_number` — Week number (default: 1)
- `metadata.lecturer_name` — Lecturer name (default: "")
- `metadata.timestamp` — Timestamp (default: current time)

### Getting Your Panopto Cookies

To authenticate with Panopto, you need to export your session cookies from your browser:

1. Open Panopto in your browser and log in
2. Open DevTools: Press `F12` (Windows) or `Cmd+Option+I` (Mac)
3. Go to **Storage** tab (Chrome/Edge) or **Cookies** tab (Firefox)
4. Find domain: `panopto.university.edu` (or your institution's Panopto domain)
5. Right-click and select "Copy All as cURL" or export as JSON
6. Save to `cookies/panopto.json`
7. **Important:** Cookies expire after ~7 days; refresh before re-running lectures

**Note:** Cookies are sensitive. Never commit `cookies/` to git (already in `.gitignore`).

### Example

See `config/example_week_05.yaml` for a complete example. Copy and customize for each lecture.

### Validation

When you run `python run_week.py config/week_05.yaml`, the system validates your config immediately:
- Missing required fields → Clear error listing what's needed
- Invalid paths → Error with full path and reason
- Non-writable directory → Error with permission details

Fix any validation errors before proceeding.

### Video Format Requirements

- Video codec: H.264 or H.265 (supported by most browsers/players)
- Duration: > 0 seconds
- File size: > ~100 MB for typical 60-min lecture (verified via ffprobe after download)

### Security & Privacy

- Cookie file stored locally, never uploaded
- Config file should NOT contain sensitive information (cookies stored separately)
- Recommended: Add `cookies/` to `.gitignore` (already done)
  </action>
  <verify>
test -f config/example_week_05.yaml (file exists)
grep -q "lecture:" config/example_week_05.yaml (contains lecture section)
grep -q "paths:" config/example_week_05.yaml (contains paths section)
grep -q "Configuration" README.md (documentation added)
grep -q "Getting Your Panopto Cookies" README.md (cookie instructions added)
  </verify>
  <done>
Example config file created and documented in README. Users can copy example_week_05.yaml and customize for their lecture. README explains all config fields, how to extract cookies, and what errors mean.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Config model validates all required fields and rejects invalid configs with clear errors
2. CLI entry point (run_week.py) successfully loads valid config, logs to file + console
3. Example config file provided and documented in README
4. Logging directory created automatically; log messages timestamped and parseable
5. All error messages are actionable (not just "error occurred"; explain what user should do)
</verification>

<success_criteria>
- [ ] ConfigModel with Pydantic validates required/optional fields correctly
- [ ] load_config() loads YAML and returns validated config or clear error message
- [ ] run_week.py accepts config file argument and validates at startup
- [ ] Logging to file + console working (both INFO level, timestamped)
- [ ] Example YAML config file provided and documented
- [ ] README explains config format, cookie extraction, and all fields
- [ ] Test: `python run_week.py config/example_week_05.yaml` succeeds with "✓ Config validated" message
- [ ] Test: Invalid config produces clear error listing missing fields or path issues
- [ ] Test: `.planning/logs/week_05.log` exists after run with timestamped entries
- [ ] All 4 CONFIG requirements (CONFIG-01, CONFIG-02, CONFIG-03, CONFIG-04) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Artifacts created (ConfigModel, load_config, example config, README section)
- Files modified (src/config.py, run_week.py, README.md, etc.)
- How downstream plans should call config validation (e.g., `from src.config import load_config`)
- Example usage: `config = load_config("config/week_05.yaml")`
- Logging setup details (location, format, how to access logs)
</output>
