---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified: ["run_week.py", "README.md", ".gitignore", "tests/test_integration.py"]
autonomous: true
requirements: [PRIV-01]
must_haves:
  truths:
    - "User can store Panopto cookies in secure config file (not committed to git, file-level permissions only)"
    - "Complete Phase 1 pipeline (config → auth → download → validate → log) works end-to-end from single CLI command"
    - "All actions logged with timestamps to file; user can trace execution and debug issues"
  artifacts:
    - path: "run_week.py"
      provides: "Complete CLI orchestration with error handling and logging"
      min_lines: 150
      exports: ["main"]
    - path: "README.md"
      provides: "Complete setup guide with cookie security, config, usage examples"
      min_lines: 200
      contains: ["Configuration", "Security", "Setup", "Cookies", "Usage"]
    - path: ".gitignore"
      provides: "Security: prevent cookies/logs from being committed"
      contains: ["cookies/", ".planning/logs/", ".env"]
    - path: "tests/test_integration.py"
      provides: "End-to-end integration tests for Phase 1 pipeline"
      min_lines: 80
  key_links:
    - from: "run_week.py main()"
      to: "config → auth → download → validate"
      via: "Sequential orchestration with error handling"
      pattern: "config.*load_config.*validate_session.*download_video"
    - from: ".gitignore"
      to: "security"
      via: "Preventing credential exposure"
      pattern: "cookies/.*logs/"
    - from: "logging setup"
      to: "debug trail"
      via: "Timestamped file + console output"
      pattern: "logging.*handlers.*FileHandler.*StreamHandler"
---

<objective>
Integrate all Phase 1 components (config, auth, download, validation, logging) into a cohesive end-to-end pipeline. Ensure security practices (cookie storage, .gitignore), comprehensive error handling, and usability (clear documentation, progress output, helpful error messages).

Purpose: Phase 1 becomes a working, production-ready foundation that users can run successfully once per week to fetch lectures.

Output: Working Phase 1 pipeline, security setup documentation, integration tests, complete README.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Complete CLI orchestration with end-to-end error handling</name>
  <files>run_week.py</files>
  <action>
Ensure `run_week.py` orchestrates all Phase 1 components with comprehensive error handling.

Main structure (already scaffolded in Plans 1-3; complete and verify):
1. Parse arguments: `python run_week.py <config_file>`
2. Setup logging: Create `.planning/logs/` directory, initialize logging to file + console
3. Load config: Validate YAML, schema, paths
4. Load cookies: Parse browser JSON export
5. Validate session: Test Panopto API with cookies
6. Download video: Stream to disk, handle network errors
7. Validate video: ffprobe checks, size/duration thresholds
8. Download transcript: Optional, graceful skip on failure
9. Print summary: List all downloaded files, log location, success status
10. Exit: 0 on success, 1 on error

Error handling (critical):
- Every step has try/except with clear error message (not stack trace for user)
- Error message includes recovery action (e.g., "Refresh cookies", "Check URL")
- Cleanup: Partial files deleted on any failure (downloader handles this)
- Logging: All errors logged at ERROR level with full context for debugging

Progress output (to console, not file):
```
🔧 Loading config from config/week_05.yaml...
✓ Config validated (Business Analytics week 5)
🔐 Testing Panopto authentication...
✓ Session valid (expires in 5 days)
📥 Downloading video...
✓ Downloaded video.mp4 (450MB)
🔍 Validating video...
✓ Video valid (62:15, H.264)
📄 Downloading transcript...
✓ Downloaded transcript.vtt
✓ Phase 1 complete
Files:
  - downloads/week_05/video.mp4 (450MB)
  - downloads/week_05/transcript.vtt
✓ Logs: .planning/logs/week_05.log
```

Logging format (to file):
```
2026-03-05 14:23:45 [INFO] Phase 1: Loading config from week_05.yaml
2026-03-05 14:23:45 [INFO] Config validated: course=Business Analytics, week=5
2026-03-05 14:23:45 [INFO] Testing Panopto session...
2026-03-05 14:23:46 [INFO] ✓ Auth valid (session expires in 5 days)
2026-03-05 14:23:46 [INFO] Downloading video...
2026-03-05 14:24:12 [INFO] ✓ Downloaded video.mp4 (450MB)
2026-03-05 14:24:12 [INFO] Validating video...
2026-03-05 14:24:13 [INFO] ✓ Video valid: H.264, duration 62:15, 450MB
2026-03-05 14:24:13 [INFO] Downloading transcript...
2026-03-05 14:24:14 [INFO] ✓ Downloaded transcript.vtt
2026-03-05 14:24:14 [INFO] Phase 1 complete
```

Implementation:
- Use logging module (stdlib) for file + console output
- Create FileHandler + StreamHandler (both output at same time)
- Catch and log all exceptions before printing user-friendly error
- Use logger.info() for progress, logger.error() for problems
- Call sys.exit(0) on success, sys.exit(1) on error

Ensure all imports are correct (config, auth, downloader, validator, logger).
  </action>
  <verify>
python run_week.py config/example_week_05.yaml (with valid config and expired/mock cookies: produces clear error, exits with code 1)
python run_week.py nonexistent.yaml (produces "Config file not found" error, exits with code 1)
# Integration test (with mocked Panopto responses):
pytest tests/test_integration.py::test_phase1_success -xvs (full pipeline succeeds)
pytest tests/test_integration.py::test_phase1_auth_failure -xvs (auth fails, clear error)
pytest tests/test_integration.py::test_phase1_download_failure -xvs (download fails, partial file cleaned up)
echo $? (verify exit code is 0 on success, non-zero on error)
  </verify>
  <done>
CLI successfully orchestrates all Phase 1 steps. Clear progress output to console, detailed logging to file. Error messages are actionable. Exit codes correct (0 on success, 1 on error). All error handling working.
  </done>
</task>

<task type="auto">
  <name>task 2: Security setup (.gitignore, cookie storage documentation)</name>
  <files>.gitignore, README.md</files>
  <action>
Ensure security best practices are documented and enforced.

Update `.gitignore`:
```
# Credentials & Secrets
cookies/
cookies.json
panopto_cookies.json

# Logs (private, not version-controlled)
.planning/logs/
*.log

# Environment variables
.env
.env.local

# Downloaded media (local, private)
downloads/
temp/
__pycache__/
*.pyc

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
```

Update README.md with security section:

```markdown
## Security & Privacy

### Cookie Storage

Panopto cookies contain your authentication session and must be stored securely:

1. **Never commit cookies to git** — Add `cookies/` to `.gitignore` (already done)
2. **File permissions** — Windows NTFS ACLs protect file (read-only for non-owner is recommended)
3. **Local storage only** — Cookies never uploaded or sent externally (except direct Panopto download)
4. **Periodic refresh** — Cookies expire after ~7 days; refresh before each lecture week

### Cookie Location

By default, cookies are stored in `cookies/panopto.json`:
- Config file specifies path: `paths.cookie_file: "cookies/panopto.json"`
- Change path in config if you prefer different location (e.g., `~/.panopto/cookies` for home dir)

### Recommended Setup

1. Create `cookies/` directory in project root: `mkdir cookies`
2. Export cookies from browser: DevTools → Storage → Export as JSON
3. Save to `cookies/panopto.json`
4. **DO NOT** edit or share this file

### Privacy Features (Phase 1)

- Raw video and audio files stored locally only (never uploaded)
- Transcript and slides stored locally only
- Cookies not logged anywhere except in local file

### Privacy Hardening (Phase 4)

- Temporary files cleaned up after processing
- PII (student names, emails) removed before LLM processing
- Google Drive sync optional (students can disable if preferred)

### Compliance Notes

- **Data residency:** All lectures stored on your local Windows machine
- **Offline-capable:** Phase 1 requires internet only for download; Phase 2+ works entirely offline
- **No external APIs in Phase 1:** Only Panopto authentication required
```

Verify:
- `.gitignore` includes `cookies/`, `.planning/logs/`, `downloads/`
- README explains why cookies are private and how to store securely
- No placeholder paths (all documented with examples)
  </action>
  <verify>
test -f .gitignore (file exists)
grep -q "cookies/" .gitignore (cookies excluded)
grep -q ".planning/logs/" .gitignore (logs excluded)
grep -q "downloads/" .gitignore (downloads excluded)
grep -q "Security" README.md (security section added)
grep -q "Cookie Storage" README.md (cookie storage documented)
grep -q "File permissions" README.md (permissions mentioned)
  </verify>
  <done>
.gitignore prevents accidental cookie/log commits. README explains security practices and why they matter. Cookie storage documented with setup instructions.
  </done>
</task>

<task type="auto">
  <name>task 3: Comprehensive README for Phase 1</name>
  <files>README.md</files>
  <action>
Complete README.md with all sections needed for users to successfully run Phase 1.

Structure:

**1. Project Overview**
```markdown
# Automated Lecture Workflow

Process weekly Panopto lectures into structured study notes in one command.

**Key Features:**
- Download video and transcript from Panopto
- Validate files automatically (ffprobe checks)
- Clear error messages and recovery instructions
- Offline-first (all files stored locally)
- Cost-effective (< $0.50/lecture using DeepSeek LLM)

**Status:** Phase 1 (Foundation) — Config, Auth, Download, Validate ✓
```

**2. Quick Start**
```markdown
## Quick Start

### Prerequisites

- Python 3.11+ (download from python.org or Windows Store)
- FFmpeg (download from gyan.dev/ffmpeg/builds/)
- pip (comes with Python)

### Installation

1. Clone repository and navigate to project:
   ```bash
   git clone <url>
   cd uni-automation
   ```

2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

3. Extract your Panopto cookies from browser (see **Configuration** below)

4. Create config file for your lecture (copy `config/example_week_05.yaml`)

### Running Phase 1

```bash
python run_week.py config/week_05.yaml
```

Expected output:
```
🔧 Loading config...
✓ Config validated
🔐 Testing authentication...
✓ Session valid
📥 Downloading video...
✓ Downloaded video.mp4 (450MB)
🔍 Validating...
✓ Video valid (62:15, H.264)
📄 Downloading transcript...
✓ Downloaded transcript
✓ Phase 1 complete
```

Files downloaded to: `downloads/week_05/`
Logs available at: `.planning/logs/week_05.log`
```

**3. Configuration (detailed, already added in Plan 1)**

**4. Getting Your Panopto Cookies (detailed, already added in Plan 1)**

**5. Common Errors & Troubleshooting**
```markdown
## Troubleshooting

### Error: "Config file not found"
- Check file path in command
- Ensure file exists: `ls config/week_05.yaml`
- Use forward slashes: `python run_week.py config/week_05.yaml`

### Error: "Cookies expired"
- Cookies are valid for ~7 days after export
- Refresh from browser: F12 → Storage → Cookies → Export
- Save to `cookies/panopto.json`
- Re-run: `python run_week.py config/week_05.yaml`

### Error: "FFmpeg not installed"
- Download from: gyan.dev/ffmpeg/builds/
- Windows: Download portable ZIP, extract, add to PATH
- Or: Set `FFMPEG_HOME` env var to ffmpeg directory

### Error: "Config validation failed"
- Check that all required fields are present:
  - `lecture.url` (Panopto video link)
  - `lecture.slide_path` (path to PDF)
  - `paths.cookie_file` (path to cookies JSON)
  - `paths.output_dir` (where to save files)
- Check that paths exist and are readable

### Error: "File size suspicious"
- Downloaded file is too small (< 100MB for typical lecture)
- This usually means:
  1. Download was interrupted (partial file)
  2. Wrong Panopto URL
  3. Video is very short (< 10 minutes)
- Try again with fresh cookies

### Error: "Cannot reach Panopto"
- Check internet connection
- Check that Panopto URL is correct
- Verify Panopto domain matches your institution

### Performance Issues

**Slow download:**
- Normal for large files (450MB ≈ 2–5 min on typical internet)
- Video downloads at network speed (no optimization in Phase 1)

**Memory usage:**
- Phase 1 uses streaming download (no RAM bloat)
- System may use 100–200 MB total (config, cookies, logging)

**Disk space:**
- Each lecture ~450–600 MB
- Ensure output directory has space before running
```

**6. File Organization**
```markdown
## Project Structure

```
├── config/
│   └── example_week_05.yaml          # Example config (copy this)
├── cookies/                           # Panopto cookies (NOT committed)
│   └── panopto.json                  # Browser cookie export
├── downloads/                         # Downloaded files
│   └── week_05/
│       ├── video.mp4                 # Panopto video
│       └── transcript.vtt             # Panopto transcript
├── .planning/
│   └── logs/
│       └── week_05.log               # Execution log
├── src/
│   ├── config.py                      # Config loading + validation
│   ├── auth.py                        # Cookie + session validation
│   ├── downloader.py                  # Video + transcript download
│   ├── validator.py                   # ffprobe validation
│   ├── logger.py                      # Logging setup
│   └── models.py                      # Data classes
├── tests/
│   ├── test_config.py
│   ├── test_auth.py
│   ├── test_downloader.py
│   ├── test_validator.py
│   └── test_integration.py
├── run_week.py                        # CLI entry point
├── requirements.txt                   # Python dependencies
├── README.md                          # This file
└── .gitignore                         # Security (prevent commits of cookies/logs)
```
```

**7. Phase 2+ Information**
```markdown
## Next Steps

Phase 1 downloads and validates media. Phase 2+ will:
- **Phase 2:** Clean transcript, extract audio, extract slide text
- **Phase 3:** Generate Feynman-structured notes using LLM (OpenRouter API)
- **Phase 4:** Add checkpointing, error recovery, privacy controls, Google Drive sync

Check `.planning/ROADMAP.md` for detailed phase breakdown.
```

**8. Development & Testing** (brief, defer full testing docs to later)
```markdown
## Testing

Unit tests for each component:

```bash
pytest tests/test_config.py -xvs          # Config validation
pytest tests/test_auth.py -xvs            # Cookie + auth
pytest tests/test_downloader.py -xvs      # Download logic
pytest tests/test_validator.py -xvs       # ffprobe validation
pytest tests/test_integration.py -xvs     # End-to-end pipeline
```

All tests should pass before proceeding to Phase 2.
```

**9. Contributing**
```markdown
## Contributing

Phase 1 is a solo project (YOLO mode). For bug reports:
- Check `.planning/logs/` for detailed error messages
- Provide log file when reporting issues
```

Ensure all sections are clear, actionable, and link together (README → Config example → Cookie instructions → Troubleshooting).
  </action>
  <verify>
grep -q "Quick Start" README.md (quick start section)
grep -q "Prerequisites" README.md (prerequisites)
grep -q "Installation" README.md (installation)
grep -q "python run_week.py" README.md (usage example)
grep -q "Troubleshooting" README.md (error handling)
grep -q "Config validation failed" README.md (specific error troubleshooting)
grep -q "Project Structure" README.md (file organization)
grep -q "Phase 2" README.md (next steps)
wc -l README.md (should be 200+ lines)
  </verify>
  <done>
README is comprehensive, clear, and actionable. Users can follow Quick Start section to set up and run Phase 1. Troubleshooting section covers common errors with recovery steps. All sections linked logically.
  </done>
</task>

<task type="auto">
  <name>task 4: Integration tests for Phase 1 pipeline</name>
  <files>tests/test_integration.py</files>
  <action>
Create comprehensive integration tests that verify the entire Phase 1 pipeline works end-to-end.

Test cases:

**test_phase1_success** (happy path):
- Create valid config file
- Create mock Panopto API responses
- Run full pipeline: config → auth → download → validate → summary
- Assert: Success exit code, all files created, logs contain key messages

**test_phase1_config_invalid**:
- Create config with missing required field
- Run pipeline
- Assert: Clear error message about missing field, exit code 1, no files downloaded

**test_phase1_auth_expired_cookies**:
- Create valid config with cookie file
- Mock Panopto API response: 401 Unauthorized
- Run pipeline
- Assert: Error message mentions cookie expiry, exit code 1, no download attempted

**test_phase1_download_network_error**:
- Config + auth valid
- Mock download to fail (ConnectionError)
- Run pipeline
- Assert: Clear network error message, partial file cleaned up, exit code 1

**test_phase1_validation_file_too_small**:
- Config + auth + download successful
- ffprobe returns small file size
- Run pipeline
- Assert: Validation error, file deleted, exit code 1

**test_phase1_transcript_optional**:
- Config + auth + download + validation successful
- Mock transcript API: 404 Not Found
- Run pipeline
- Assert: Warning logged, pipeline continues, success exit code 0

Structure:
```python
# tests/test_integration.py
import pytest
from unittest.mock import patch, MagicMock
from pathlib import Path
import tempfile
import json

class TestPhase1Integration:
    @pytest.fixture
    def temp_dir(self):
        # Temp directory for test files
        with tempfile.TemporaryDirectory() as tmpdir:
            yield Path(tmpdir)
    
    @pytest.fixture
    def valid_config(self, temp_dir):
        # Create valid config for testing
        config = {
            "lecture": {"url": "https://panopto.edu/...", "slide_path": str(temp_dir / "slides.pdf")},
            "paths": {"cookie_file": str(temp_dir / "cookies.json"), "output_dir": str(temp_dir / "output")},
            "metadata": {"course_name": "Test", "week_number": 1}
        }
        # Create example files
        (temp_dir / "slides.pdf").touch()
        (temp_dir / "cookies.json").write_text(json.dumps([{"name": "cookie", "value": "test"}]))
        return config
    
    def test_phase1_success(self, temp_dir, valid_config):
        # Mock Panopto responses
        with patch('requests.get') as mock_get:
            # Mock auth response
            mock_auth = MagicMock()
            mock_auth.status_code = 200
            
            # Mock video download
            mock_video = MagicMock()
            mock_video.status_code = 200
            mock_video.iter_content.return_value = [b'fake_video_data']
            
            mock_get.side_effect = [mock_auth, mock_video]
            
            # Run pipeline
            # Assert success
    
    def test_phase1_config_invalid(self, temp_dir):
        # Invalid config (missing required field)
        # Assert error message
    
    # More tests...
```

Mocking strategy:
- Use `unittest.mock.patch` to mock requests.get, subprocess.run (ffprobe)
- Mock responses with appropriate status codes and data
- Test error cases by simulating failures
- Use temporary directories for file I/O (cleanup automatic)

Assertions:
- Exit code (0 on success, 1 on error)
- Error message content (specific error mentioned)
- File existence (videos/transcripts created on success, deleted on failure)
- Log file content (key steps logged)

Implementation:
- Import from src modules (config, auth, downloader, validator)
- Create minimal valid config + cookies for testing
- Mock external APIs (Panopto, ffprobe)
- Run main() or individual functions and assert results
- Keep tests fast (< 5 sec per test with mocking)
  </action>
  <verify>
pytest tests/test_integration.py -xvs (all tests pass)
pytest tests/test_integration.py::test_phase1_success -xvs (happy path passes)
pytest tests/test_integration.py::test_phase1_auth_expired_cookies -xvs (error handling passes)
pytest tests/test_integration.py -k "error" (all error cases pass)
  </verify>
  <done>
Integration tests cover happy path and all major error cases. Tests are fast (use mocking), isolated (no external API calls), and comprehensive (config, auth, download, validate, logging all tested).
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. run_week.py orchestrates all Phase 1 steps with comprehensive error handling
2. Progress output to console, detailed logging to file
3. .gitignore prevents credentials/logs from being committed
4. README covers setup, configuration, cookie extraction, troubleshooting, and file organization
5. Integration tests verify entire pipeline works end-to-end
6. All error messages are clear and actionable
7. PRIV-01 (cookie storage with file permissions) addressed
</verification>

<success_criteria>
- [ ] run_week.py successfully orchestrates config → auth → download → validate → summary
- [ ] Clear progress output to console (emoji + messages)
- [ ] Detailed logging to file with timestamps
- [ ] Error messages are actionable (not just "error"; explain recovery)
- [ ] Exit codes correct (0 on success, 1 on error)
- [ ] .gitignore prevents cookies/, logs/, downloads/ from being committed
- [ ] README has Quick Start, Configuration, Cookie Instructions, Troubleshooting sections
- [ ] README has clear file organization and directory structure
- [ ] Integration tests pass (happy path, auth error, download error, validation error, transcript optional)
- [ ] Test: `python run_week.py config/example_week_05.yaml` produces expected output format
- [ ] Test: Invalid config produces clear error listing what's missing
- [ ] Test: Expired cookies produce "Cookies expired. Refresh from browser..." message
- [ ] All logging appears in both console and file
- [ ] PRIV-01 requirement addressed (cookies stored in config file, git-ignored, file permissions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` with:
- Final status: Phase 1 complete, all 12 requirements addressed
- Files created/modified (complete list)
- How to run Phase 1 (quick reference)
- How Phase 2 should build on Phase 1 (dependency info)
- Key artifacts available to downstream phases (imports, exports)
- Testing status (integration tests passing, coverage of happy path + main error cases)
- Known limitations (Phase 1 happy-path only, no checkpointing, no retry logic)
</output>
